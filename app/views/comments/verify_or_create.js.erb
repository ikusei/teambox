<% if @success %>
	$('popupwindow').update("Gespeichert").highlight().blindUp();
<% else %>
	$('popupwindow').update("<%= escape_javascript(render(:partial => '/comments/comment_form'))%>");
	$("comment_project_id").highlight();
	$("comment_target_id").highlight();
<% end %>